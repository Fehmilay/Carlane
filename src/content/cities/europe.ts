import type { CityDef } from '../../core/types';
import type { SkylineFn } from './types';
// STUB — replaced by the Europe catalog: duesseldorf, berlin, munich, istanbul, paris, london, rome, amsterdam, barcelona, moscow, reykjavik.
export const CITIES_EUROPE: CityDef[] = [];
export const SKYLINES_EUROPE: Record<string, SkylineFn> = {};
